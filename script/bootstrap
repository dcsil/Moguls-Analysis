# enter the directory that bootstrap is in
cd "$(dirname "$(find . -name 'bootstrap')")"
# get current directory
DIR=$PWD

echo "==================== Dev Environment Setup ===================="
$DIR/env_setup

echo "==================== App Setup ===================="
$DIR/app_setup

echo "==================== Setup Success! ===================="
